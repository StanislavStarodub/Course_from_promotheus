The script generates a list of rows that represent the stages of the life cycle of software development: “plan”, “code”, “test”, “delivery”, “deploy” and “monitor”, and displays each element of the list in the view of the row in the format base64.
